/ %h2= チェク・リスト

/ = form_with url: source_index_path, method: :get, local: true do |f|
/   = f.date_field :date, value: @select_date
/   = f.submit :submit
/ = f.label :list_id, '便選択'
/ = f.collection_select :list_id, List.group(:name).order(:name), :id, :name, include_blank: :true

%table{border: 1}
  %tr
    %th 便名
    %th ファイル名 (URLも設定されている場合、こちらが優先される)
    %th 接続先URL
    %th 
    %th 
  / - @list.items.each do |item|
  /   %tr
  /     %td= item.name
  /     %td= link_to item.file, item.file.url, target: '_blank'
  /     %td= link_to item.url, item.url, target: '_blank'
  /     %td= link_to '編集', item_edit_path(item)
  /     %td= link_to '削除', item_delete_path(item), method: :delete, data: {confirm: '削除しますか？'}
